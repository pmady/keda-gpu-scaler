bin/
/keda-gpu-scaler
*.exe
*.test
*.out
.DS_Store
vendor/
promotion/
dev-server/
Dockerfile.dev
terraform/
# but track the committed Infrastructure-as-Code (see infra/terraform/.gitignore
# for the Terraform artifacts that are still ignored within it)
!infra/terraform/
dev/
.idea
